int global_var;

int gcd(int u, int v) {
    if (v == 0) return u;
    else return gcd(v, u - u / v * v);
}

void main() {
    int x;
    int y;
    x = 10;
    y = 20;
    while (x < y) {
        x = x + 1;
    }
    return;
}